1. Access the client directory `cd safety-bnb/client` and run `npm install`
2. Start the client: `npm start`
3. Access the server directory `cd safety-bnb/server` and run `npm install`
4. Start the server: `npm start`
